{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "if [ -d web ]; then cd web; fi && npm install --legacy-peer-deps",
  "buildCommand": "if [ -d web ]; then cd web; fi && npm run build",
  "outputDirectory": "web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
