# Preferred: leave both unset and proxy /api + WebSocket traffic through the
# frontend origin. Only use direct URLs for same-site subdomains, and add this
# frontend origin to the backend FRONTEND_ORIGINS setting.
# VITE_API_BASE_URL=https://api.example.com
# VITE_WS_BASE_URL=wss://api.example.com
